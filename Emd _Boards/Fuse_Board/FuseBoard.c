#include<stdio.h>
#include"FuseBoard.h"

int main()
{
    printf("This is the SW version for FuseBoard that is fetched: %s\n",SW_BUILD_VERSION);
    printf("This is the SW version V2  for FuseBoard that is fetched: %s\n",SW_BUILD_VERSION);
}