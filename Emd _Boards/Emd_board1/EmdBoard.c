#include<stdio.h>
#include"EmdBoard.h"

int main()
{
    printf("This is the SW version that is fetched: %s\n",SW_BUILD_VERSION);
    printf("This is the SW version that is fetched: %s\n",SW_BUILD_VERSION);


}